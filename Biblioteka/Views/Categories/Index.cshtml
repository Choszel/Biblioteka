@model IEnumerable<Biblioteka.Models.Category>

@{
    ViewData["Title"] = "Index";
}

<h1>Kategorie</h1>

<p>
    <a asp-action="Create" class="customButton">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.catName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.catName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.catId" class="customButton">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.catId" class="customButton">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.catId" class="customButton">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
